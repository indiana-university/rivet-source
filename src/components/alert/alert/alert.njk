<div class="rvt-alert rvt-alert--{{ type }}{% if classes %} {{ classes }}{% endif %}" role="alertdialog" aria-labelledby="{{ id }}-alert-title" data-rvt-alert="{{ type }}">
    <div class="rvt-alert__title" id="{{ id }}-alert-title">{{ title }}</div>
    <p class="rvt-alert__message">{{ message }}</p>
    {% render "@includes--alert-close-button" %}
</div>