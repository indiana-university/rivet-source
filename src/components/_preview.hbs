<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="shortcut icon" href="{{ path '/favicon.ico' }}">
    <link media="all" rel="stylesheet" href="{{ path '/css/style.css' }}">
    <title>Preview Layout</title>
</head>
<body>
    <div class="display-wrapper">
        {{{ yield }}}

        <style scoped>
            /**
             * Using these styles for display purposes only. We don't want them
             * to end up in the final compiled CSS for distribution.
             * For reference:
             * http://w3c.github.io/html-reference/style.html#style.attrs.scoped
             * https://css-tricks.com/saving-the-day-with-scoped-css/
             */
            .display-wrapper {
                padding: 3rem;
                background-color: blue;
            }

            .display-wrapper input,
            .display-wrapper select,
            .display-wrapper textarea {
                max-width: 36rem;
            }
        </style>
    </div>
</body>
</html>
