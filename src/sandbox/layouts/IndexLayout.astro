---
import { baseUrl } from "@src/utilities/baseUrl.js";
import Layout from "./SandboxLayout.astro";
const { children } = Astro.props;
---

<Layout {...Astro.props}>
	<ul>
		{
			children.map((page) => (
				<li>
					<a href={baseUrl(page.path)}>{page.title}</a>
				</li>
			))
		}
	</ul>
</Layout>
