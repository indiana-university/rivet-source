---
let module = null;
if (import.meta.env.DEV) {
	module = await import("./BaseLayoutDev.astro");
} else {
	module = await import("./BaseLayoutPreview.astro");
}
const { default: Layout } = module;
---

<Layout {...Astro.props}>
	<slot />
</Layout>
