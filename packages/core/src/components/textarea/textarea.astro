---
import { createId } from "../../utilities/createId.js";

export const title = "Textarea";

const textareaId = `textarea-default-${createId()}`;
---

<label class="rvt-label" for={textareaId}>Textarea</label>
<textarea class="rvt-textarea" id={textareaId}></textarea>
