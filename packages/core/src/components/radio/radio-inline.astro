---
import { createId } from "../../utilities/createId.js";

export const title = "Inline Radio";

const radioOneId = `radio-inline-one-${createId()}`;
const radioTwoId = `radio-inline-two-${createId()}`;
---

<fieldset class="rvt-fieldset">
	<legend>Radio input (inline list)</legend>
	<ul class="rvt-list-inline">
		<li>
			<div class="rvt-radio">
				<input type="radio" name="radio-group-1" id={radioOneId} />
				<label for={radioOneId}>Option one</label>
			</div>
		</li>
		<li>
			<div class="rvt-radio">
				<input type="radio" name="radio-group-1" id={radioTwoId} />
				<label for={radioTwoId}>Option two</label>
			</div>
		</li>
	</ul>
</fieldset>
