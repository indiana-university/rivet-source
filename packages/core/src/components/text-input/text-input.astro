---
import { createId } from "../../utilities/createId.js";

export const title = "Text Input";

const inputId = `text-input-default-${createId()}`;
---

<label for={inputId} class="rvt-label">Text Input</label>
<input type="text" id={inputId} class="rvt-text-input" />
